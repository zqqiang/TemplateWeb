const initialSettings = {
  locale: {
    languageId: "english",
    locale: "en",
    name: "English",
    icon: "us"
  }
};

const settings = (state = initialSettings, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default settings;
